=f.input :character
=f.input :category, :as => :select, :collection => ["Attendance","Downtime","Spend","Error","Other"], :value_method => :to_s
=f.input :reason
=f.input :amount
-if current_user.role == "admin"
  =f.input :user
-else
  =f.input :user_id, :as => :hidden, :value=> current_user.id
